[project]
name = "evaluate-suites-from-yaml-tutorial"
version = "0.0.0"
description = "Evaluate gllm-evals suites defined in YAML"
requires-python = ">=3.11,<3.14"
readme = "README.md"
dependencies = [
    "python-dotenv>=1.0.0,<2.0.0",
]

[tool.uv.sources]
gllm-evals = { path = "C:/Users/kalvi/gl-sdk/libs/gllm-evals", editable = true }
gllm-inference = { path = "C:/Users/kalvi/gl-sdk/libs/gllm-inference", editable = true }

[tool.uv]
package = false
