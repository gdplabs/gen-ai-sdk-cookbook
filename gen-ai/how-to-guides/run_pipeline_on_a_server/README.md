## ⚙️ Prerequisites

Please refer to prerequisites [here](../README.md).

## 🚀 Getting Started

1. **Clone the repository & open the directory**

   ```bash
   git clone https://github.com/gdplabs/gen-ai-sdk-cookbook.git
   cd gen-ai-sdk-cookbook/gen-ai/how-to-guides/run_pipeline_on_a_server
   ```

2. **Set UV authentication and install dependencies**

   **For Unix-based systems (Linux, macOS):**
   ```bash
   ./setup.sh
   ```

   **For Windows:**
   ```cmd
   setup.bat
   ```

3. **Run the server**

   ```bash
   uv run uvicorn main:app --reload
   ```

4. **Run the client in another terminal**

   ```bash
   uv run run.py
   ```

5. **Expected Output**

   ```text
   Expected output will be added after verification.
   ```

## 📚 Reference

These examples are based on the [GL SDK GitBook documentation](https://gdplabs.gitbook.io/sdk/gen-ai-sdk/guides/run-pipeline-on-a-server).
